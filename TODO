Socket lib
  - refactor to use getaddrinfo
  - go through and make sure things are checked for error return values
  - Use enum or something instead of such much virtual function 

Parsing
  - add support for tags

IRC Commands
  - NICK: set nickname
  - USER: set username, realname and usermode
  - PASS: set password
  - PRIVMSG: message a target user or channel
  - JOIN: join a channel

UI
	- custom element for messages 
