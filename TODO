Socket lib
  - refactor to use getaddrinfo
  - go through and make sure things are checked for error return values
  - Use enum or something instead of such much virtual function 

Parsing
  - add support for tags

UI 
	- fucking settings or something
	- perchance revive the vim dream
	- colour scheme (and other settings but this first bc preeeety) loaded from config file into unorederd map i think

IRC Commands
  - NICK: set nickname
  - USER: set username, realname and usermode
  - PASS: set password
  - PRIVMSG: message a target user or channel
  - JOIN: join a channel
